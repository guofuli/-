git命令

git init
初始化一个本地GIT仓库

git status
查看本地仓储的状态
第一次查看，显示的是一坨没有被跟踪的状态

git status -s
-s是输出简要的变更日志


git add
可以将一个没有被跟踪的文件添加到跟踪列表
类似于node_modules这种形状的文件是不应该被跟踪
例子：
git add --all

在代码库文件夹的根目录添加一个.gitignore文件
此文件用于说明忽略的文件有哪些


git commit
将本地的变化提交到本地的仓库文件夹归档
例子：git commit -m ‘提交说明内容’

git diff
比较


git log
查看提交日志


git reset --hard 2d2250
恢复到上一个版本

git branch
查看当前分支
git branch v2
创建一个新的分支

git check out 分支名字
切换到那个分支


git push -u orgin 分支名字
上传到新的分支

git clone  网址




doc环境下进入要操作的路径
git init

git status

git add --all

git commit -m "提交说明"

在github上创建一个仓库

git remote add orgin 网址

git push -u orgin master


创建一个新的分支
git branch  gh-pages


切换到当前分支
git checkout gh-pages

git  push -u orgin gh-pages
更新到当前分支

地址。名字.github.io/项目名字
例如：michua.github.io/wjs



